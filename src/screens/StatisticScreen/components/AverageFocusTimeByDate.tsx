import {StyleSheet, View} from 'react-native';

const AverageFocusTimeByDate = () => {
  return <View></View>;
};

export default AverageFocusTimeByDate;

const styles = StyleSheet.create({
  placeholder: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
